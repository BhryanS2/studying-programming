#include<bits/stdc++.h>
using namespace std;

int main() {
  int n; cin >> n;
  vector<int> resp(n + 1, -1);
  int esq=2, direita = n - 1;
  resp[0] = 1;
  resp[n-1] = 2;
  while (n)
  {
    
  }
  
  
}